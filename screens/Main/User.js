import { SafeAreaView, Text } from "react-native";

export default function User ({navigation}) {
    return (
        <SafeAreaView>
            <Text>
                User
            </Text>
        </SafeAreaView>
    )
}