import { SafeAreaView, Text } from "react-native";

export default function Library ({navigation}) {
    return (
        <SafeAreaView>
            <Text>
                Library
            </Text>
        </SafeAreaView>
    )
}